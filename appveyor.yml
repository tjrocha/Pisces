version: 1.0.{build}
image: Visual Studio 2022

branches:
  only:
    - master
    - appveyor

#install:
#  - cmd: appveyor DownloadFile https://dist.nuget.org/win-x86-commandline/latest/nuget.exe 
  
before_build:
  - cmd: nuget restore Pisces.sln

configuration: Debug
platform: Any CPU 

build:
  project: Pisces.sln
  verbosity: minimal
  
test:
  assemblies:
    ## Test the libraries
    - Core\bin\Debug\net4.8\Reclamation.Core.dll
    - TimeSeries\bin\Debug\net4.8\Reclamation.TimeSeries.dll

  categories:
    except:
    - DatabaseServer
    - Internal