version: 1.0.{build}
image: Visual Studio 2022

branches:
  only:
    - master
    - appveyor

#install:
#  - cmd: appveyor DownloadFile https://dist.nuget.org/win-x86-commandline/latest/nuget.exe 
  
before_build:
  - cmd: nuget restore Pisces.sln

configuration: Debug
platform: Any CPU 

build:
  project: Pisces.sln
  verbosity: minimal

test_script:
  - cmd: vstest.console TimeSeries\bin\Debug\Reclamation.TimeSeries.dll  
  - cmd: vstest.console Core\bin\Debug\Reclamation.Core.dll  
